<% content_for(:title, t('.title')) %>
<div class="max-w-lg mx-auto mt-10 p-6 bg-white shadow-md rounded-md">
  <h1 class="text-2xl font-semibold text-gray-800 text-center mb-6"><%= t('.title') %></h1>
  <%= form_with url: login_path, class: "space-y-6" do |f| %>
    <div>
      <%= f.label :email, class: "block text-sm font-medium text-gray-700" %>
      <%= f.email_field :email, class: "mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring focus:ring-beige-500 focus:border-beige-500" %>
    </div>
    
    <div>
      <%= f.label :password, class: "block text-sm font-medium text-gray-700" %>
      <%= f.password_field :password, class: "mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring focus:ring-beige-500 focus:border-beige-500" %>
    </div>

    <div class="text-center">
      <%= f.submit t('.title'), class: "w-full bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-md transition duration-200" %>
    </div>
  <% end %>

  <div class="mt-4 text-center space-x-4">
    <%= link_to t('.to_register_page'), new_user_path, class: "text-sm text-gray-600 hover:underline" %>
    <%= link_to t('.password_forget'), password_resets_new_path, class: "text-sm text-gray-600 hover:underline" %>
  </div>
</div>